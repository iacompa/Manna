/**
 * Settings contract anchor (AGENTS.md). Flag-off builds re-export the inert default only.
 * When enabling BYOK for an internal build, swap to ai-provider.byok.tsx.example pattern.
 */
export { default } from "./ai-provider.default";
