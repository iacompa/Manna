# Figma — Manna design source

## File

| Field | Value |
| --- | --- |
| URL | https://www.figma.com/design/2QHxehl21MVYrPeLKtGZuc |
| `fileKey` | `2QHxehl21MVYrPeLKtGZuc` |
| Product | Manna (iOS / Android store app) |

## Phase 0 status (honest)

**Not design-complete.** Journey frames, screen flows, and component specs for implementation handoff are **not** in the file yet.

What exists today (per owner review):

- Foundations work in progress: color, type, and spacing tokens where Starter quota allows
- No full bilingual journey frames (Home, Bible reader, Prayer, Search, Settings, Plus paywall, AI surfaces)

## Blocker

Figma **Starter** plan quota blocks creating the full file structure and frames needed for Phase 0 exit (“Figma foundations”). Upgrade to **Figma Professional** (or equivalent team seat) is required before the design agent can run `create_new_file` / journey frames per orchestration plan Packet 0C.

**Do not** treat tokens-only work as “Figma complete” for merge gates or Phase 1 UI implementation.

## Authority

When screens land in Figma, they sit below `PLAN.md` and ADRs and above ad-hoc implementation. Record major token or IA changes in the plan decision log.

## MCP usage (when unblocked)

Design workers use Figma MCP (`search_design_system`, file tools) with `figma-use` / `figma-generate-design` skills. Implementation agents consume this doc + Figma links; they do not invent screens from prose alone.

## Next steps when unblocked

1. Confirm file access for design + mobile lanes
2. Publish EN/ES parity checklist for each journey
3. Link component set IDs in this doc (append-only changelog section below)

### Changelog

| Date | Note |
| --- | --- |
| 2026-07-12 | File URL and `fileKey` recorded; tokens/type only; journeys blocked on Starter quota |
